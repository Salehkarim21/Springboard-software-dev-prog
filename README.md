# Springboard-software-dev-prog


Part II
man command will give details about what a command does (the command typed after man)
exit man by presing on Q. we can scroll throu using arrows and short cuts like ctrl+a  ctrl+e ctrl+arrow left/right
ctrl+e will take us to end of the line in terminal. ctrl+left/right lets us jump between words.
ctrl+a moves cursor to begining of terminal
ctrl+w deletes words in termianl ctrl+u deletes lines
terminal is command line interface to the shell, the shell is the program that does the commands. shell runs in the terminal.
Absolute path is the path that starts from teh root and give entire path location of the folder/file
Relative the path from current directory
A flag is synopsis that adds to the function of teh commnd, ex -a -l -r
-r will remove the empty folder, -f will force remove empty or not.